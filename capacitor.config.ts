import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.visicoders.collegemanagement',
  appName: 'College Management',
  webDir: 'public'
};

export default config;
